#pragma once

#if !defined(PORT_PP)
# error PORT_PP not configured for target
#endif

#if !defined(DDR_PP)
# error DDR_PP not configured for target
#endif

#if !defined(PIN_PP)
# error PIN_PP not configured for target
#endif

#if !defined(PORT_OD)
# error PORT_OD not configured for target
#endif

#if !defined(DDR_OD)
# error DDR_OD not configured for target
#endif

#if !defined(PIN_OD)
# error PIN_OD not configured for target
#endif

#if !defined(PIN_NO_INT)
# error PIN_NO_INT not configured for target
#endif

#if !defined(PORT_INT)
# error PORT_INT not configured for target
#endif

#if !defined(DDR_INT)
# error DDR_INT not configured for target
#endif

#if !defined(PIN_INT)
# error PIN_INT not configured for target
#endif
